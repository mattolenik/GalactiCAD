# Pinned stable (matches the installed 1.96.0). Used for both the native kernel
# build/test path and the WASM artifact (`make gcad-wasm` via wasm-pack) — no
# nightly toolchain is required.
[toolchain]
channel = "1.96.0"
